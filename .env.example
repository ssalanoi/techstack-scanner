# JWT Configuration
# Generate a secure 256-bit secret key (minimum 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secure-256-bit-secret-key-change-this-immediately-min-32-chars

# Admin Credentials
ADMIN_EMAIL=admin@techstack.local
ADMIN_PASSWORD=ChangeMe123!

# Ollama Configuration
OLLAMA_MODEL=llama3.2
# Alternative models: mistral, codellama, llama2

# Database Configuration (optional override)
# CONNECTION_STRING=Data Source=/app/data/scan.db

# Environment
ASPNETCORE_ENVIRONMENT=Production

# API Configuration
API_PORT=5000

# Web Configuration
WEB_PORT=3000

# Notes:
# 1. Copy this file to .env and update the values
# 2. NEVER commit .env to version control
# 3. JWT_SECRET must be at least 32 characters
# 4. Use strong passwords for production
# 5. After changing credentials, restart containers: docker compose down && docker compose up -d
